<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.accessor.mapper.MeasureMapper">
    <insert id="insert">
        insert into mysql.tb_measure (project_id, measure_name, DI)
        values (#{projectId},#{measureName},#{DI});
    </insert>
</mapper>